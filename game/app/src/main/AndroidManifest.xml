<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.groupc.android.illuminati">

    <application
        android:allowBackup="true"
        android:icon="@drawable/illuminati_logo_5"
        android:roundIcon="@drawable/illuminati_logo_5"
        android:label="Illuminice"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <service android:name=".PlayerNames" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainScreen" />
        <!--<activity android:name=".PlayerList"></activity>-->
    </application>

</manifest>